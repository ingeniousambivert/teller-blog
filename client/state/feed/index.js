import { atom } from "recoil";

export const articlesState = atom({
    key: "articlesState",
    default: []
});
export const categoriesState = atom({
    key: "categoriesState",
    default: []
});
